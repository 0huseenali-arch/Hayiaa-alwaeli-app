<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- ===== الأذونات ===== -->

    <!-- الاتصال بالإنترنت -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- الموقع: لحساب مواقيت الصلاة واتجاه القبلة -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- عدّاد الخطى -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- الإشعارات (أندرويد 13 فما فوق) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- الاهتزاز عند الضغط والتسبيح وعند الأذان -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- إبقاء المعالج يقظاً أثناء تشغيل الأذان والمحاضرات -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ===== الأذان: تنبيه في وقت محدّد بدقّة ===== -->
    <!-- SCHEDULE_EXACT_ALARM: يطلبه التطبيق من المستخدم عند الحاجة -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- إعادة جدولة الأذان تلقائياً بعد إعادة تشغيل الهاتف -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ===== الحساسات: اختيارية حتى لا تستبعد Google Play الأجهزة التي تفتقدها ===== -->
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
